---
name: Build and push image

'on':
  workflow_dispatch:
    inputs:
      target:
        description: The upstream image to build from
        required: true
        type: string

      version:
        description: The upstream version to build from
        required: true
        type: string

permissions:
  id-token: write
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4
        with:
          persist-credentials: false

      - name: Login to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup ASDF
        uses: ./.github/actions/setup-asdf

      - name: Build image
        run:
          bin/bix docker build-image ${{ inputs.target }} ${{ inputs.version }}

      - name: Push image
        run:
          bin/bix docker push-image ${{ inputs.target }} ${{ inputs.version }}
