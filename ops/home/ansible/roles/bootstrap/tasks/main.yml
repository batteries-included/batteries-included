---
- name: "Update apt cache"
  become: true
  apt:
    update_cache: true
    cache_valid_time: 3600

- name: "Upgrade apt packages"
  become: true
  apt:
    upgrade: safe

- name: Ensure required packages
  apt: pkg="{{ fail2ban_debian_packages }}" state=present
