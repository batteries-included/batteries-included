<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "raw_config-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :path %>
  <%= text_input f, :path %>
  <%= error_tag f, :path %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
