---
// CSS variables are now defined in tailwind.css using @theme directive
// Fonts are imported via @import in tailwind.css
---

<style is:inline is:global>
  :root {
    ::selection {
      background-color: rgb(252 64 139);
    }
  }
</style>
