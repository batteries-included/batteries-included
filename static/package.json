{
  "scripts": {
    "build-and-serve": "npm run build && npm run serve",
    "build:preview": "hugo --cleanDestinationDir --minify --gc -DFE --baseURL \"${DEPLOY_PRIME_URL:-/}\"",
    "build:production": "hugo --cleanDestinationDir --minify --gc",
    "build": "hugo --cleanDestinationDir -DFE --gc",
    "hugo:version": "hugo version",
    "serve": "hugo serve --disableFastRender -DFE "
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^4.1.3",
    "@tailwindcss/forms": "^0.5.2",
    "@tailwindcss/typography": "^0.5.4",
    "autoprefixer": "^10.4.7",
    "postcss": "^8.4.14",
    "postcss-cli": "^10.0.0",
    "tailwindcss": "^3.1.6"
  }
}
